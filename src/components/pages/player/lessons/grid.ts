/**
 * Classes da grade de aulas.
 *
 * Fora do módulo da lista (que é `"use client"`) para o esqueleto da página,
 * renderizado no servidor, poder usar exatamente a mesma grade.
 */
export const GRID = "grid gap-6 sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4";
