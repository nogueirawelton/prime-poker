/** `"Felipe Martins"` → `"FM"`. Espaços repetidos não geram letra vazia. */
export function initials(name: string) {
  return name
    .split(" ")
    .filter(Boolean)
    .slice(0, 2)
    .map((part) => part[0])
    .join("")
    .toUpperCase();
}
